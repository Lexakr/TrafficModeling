<Window x:Class="TrafficModeling.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficModeling"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TabControl Name="Tab_Model">
            <TabItem Header="Параметры Модели">
                <Grid Background="#FFE5E5E5" Height="405">
                    <Image Margin="0,10,10,10" Source="/road.jpeg" HorizontalAlignment="Right" Width="327"/>
                    <Button Content="Старт" Margin="24,0,0,32" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="75" Click="Button_Click"/>
                    <Button Content="Сброс" Margin="117,0,0,32" HorizontalAlignment="Left" Width="75" Height="40" VerticalAlignment="Bottom"/>
                    <TextBox HorizontalAlignment="Left" Margin="83,39,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
                </Grid>
            </TabItem>
            <TabItem Header="Статистика">
                <Grid Background="#FFE5E5E5" Margin="0,0,0,58">
                    <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right" >
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Sales" LabelFormatter="{Binding YFormatter}"></lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Month" Labels="{Binding Labels}"></lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </TabItem>
            <TabItem Header="+">

            </TabItem>
        </TabControl>
    </Grid>
</Window>
